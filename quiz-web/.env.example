# URL only visible to the Next.js server. Browser requests use /api.
API_ORIGIN=http://localhost:8082
# Public origin used by the server-side BFF to reject cross-site mutations.
APP_ORIGIN=http://localhost:3000
